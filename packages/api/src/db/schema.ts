import { appSchema } from "@flintapi/shared/Utils";

export const {
  selectOrganization,
  verification,
  user,
  organization,
  invitation,
  session,
  account,
  apiKey,
  member
} = appSchema

export {appSchema};
